package main 

import (
	"fmt"
)

var y = 32

func main()  {
	//colorred = 
	fmt.Printf("Em hexadecimal com ax %#x, em binario %b e em hexadecimal %x\n", y, y, y)
	fmt.Printf("O valor de y é %v, já os demais valores são: %#x\t%b\t%x\n", y, y, y, y)

}